{% assign menu = linklists.main-menu.links %}
<nav>
    <ul class="remove-bullets-from-list">
        {% for link in menu %}
            <li>
                <a href="{{ link.url }}">{{ link.title }}</a>    
            </li>
        {% endfor %}
    </ul>
</nav>
